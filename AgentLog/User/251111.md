# User 251111

## Start Log
- 20251111 (세션 시작 시각 불명)
- 작업자 : User

### To Do

1. InteractableComponent의 Server RPC 문제 해결
2. 멀티플레이 환경에서 물체 PickUp/Drop 동작 구현
3. 서버의 Camera Pitch Replication 문제 해결

---

## Compact Log 1
- 20251111 00:59

### 요약 내용

**1. InteractableComponent Attach 버그 수정**
- `InteractableComponent.cpp:56` 라인에서 `MyPlayer->AttachToComponent()` 호출 오류 발견
- 문제: Actor를 자기 자신의 컴포넌트에 attach 시도
- 해결: `GetOwner()->AttachToComponent()`로 수정하여 카드 액터를 플레이어의 HoldPosition에 정상적으로 attach

**2. Server RPC 실행 문제 해결**
- 클라이언트에서 `Server_PickUp()` RPC가 실행되지 않는 문제 발견
- 원인: Card Actor의 Owner가 설정되기 전에 RPC 호출
- 해결: MyPlayerController에 `Server_Grab()` / `Server_Release()` RPC 추가
  - 입력을 서버에서 먼저 처리하도록 변경
  - `MyPlayerController::Server_Grab_Implementation()`에서 `TryPickUp()` 호출
  - 이를 통해 모든 PickUp 로직이 서버에서 실행되도록 보장

**3. Camera Pitch Replication 시도 (미해결)**
- 문제: 서버가 물체를 들고 상하로 움직일 때 클라이언트에서 상하 움직임이 복제되지 않음
- 시도한 방법들:
  1. `APawn::RemoteViewPitch` 활용 시도 → 변수명 충돌로 컴파일 에러
  2. `ReplicatedCameraPitch` 변수 추가 및 `Replicated` 속성 사용 → 값이 0과 실제값 사이를 왔다갔다함
  3. `ReplicatedUsing=OnRep_CameraPitch` 사용 시도 → 여전히 미해결
- 디버그 로그 결과:
  - 서버: Pitch 값 정상 업데이트 확인
  - 클라이언트: 복제된 값이 0.000000과 실제 값이 번갈아 나타남 (Tick과 복제 타이밍 불일치)
- **상태: 미해결, 추가 조사 필요**

### Commit 정보
"Implement multiplayer interaction system for card pickup"

1. "Fix InteractableComponent attach error"
   - InteractableComponent.cpp에서 잘못된 Actor attach 호출 수정
   - MyPlayer를 자신에게 attach하던 것을 GetOwner()를 사용하도록 변경

2. "Add Server RPC for player input handling"
   - MyPlayerController에 Server_Grab 및 Server_Release RPC 추가
   - 입력 처리를 서버에서 먼저 수행하도록 아키텍처 변경
   - Card Actor의 Owner 설정 문제 해결

3. "Attempt Camera Pitch replication (WIP)"
   - ReplicatedCameraPitch 변수 추가 및 OnRep 함수 구현
   - 서버의 상하 시점 변화를 클라이언트에 복제 시도
   - 현재 미해결 상태로 추가 디버깅 필요

---

## End Log
- 20251111 00:59
- 작업자 : User

### 오늘 한 일

1. "멀티플레이 Interaction 시스템 기본 구현"
   - InteractableComponent의 PickUp/Drop RPC 구조 수정
   - MyPlayerController를 통한 Server-side 입력 처리 구현
   - Card pickup 기능 정상 작동 확인

2. "Camera Pitch Replication 문제 파악"
   - 서버의 상하 시점이 클라이언트에 복제되지 않는 문제 발견
   - 여러 해결 방법 시도했으나 미해결
   - 복제 타이밍 이슈로 추정, 내일 추가 작업 필요

### 미해결 이슈

- **Camera Pitch Replication**: 서버가 물체를 들고 위아래로 움직일 때 클라이언트에서 상하 움직임이 보이지 않음
  - 디버깅 결과 복제는 발생하나 Tick과 타이밍 불일치
  - 다음 세션에서 다른 접근 방법 검토 필요 (예: SpringArm의 bUsePawnControlRotation 대신 수동 회전 제어)
