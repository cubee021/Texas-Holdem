# cubee021 251231

# Start Log
- 20251231 오후 (세션 시작)
- 작업자 : cubee021

### To Do

**긴급 버그 수정**
1. "베팅 UI 버튼 구성 버그 수정"
    1. 버그 재현 상황 분석
        - Turn 페이즈에서 상대가 Check 후, 현재 플레이어 턴
        - UI에 [Bet 5, Call 0, Fold] 표시됨
        - Call 금액이 0인 상황에서 Bet과 Call이 동시에 표시
    2. 관련 코드 확인
        - 베팅 UI 업데이트 로직 분석
        - 버튼 구성 결정 로직 파악
    3. 원인 파악
        - Call 금액이 0일 때 Check로 표시되어야 하는데 Call로 표시되는 원인
        - Bet과 Call/Check가 동시에 표시되는 조건 분석
    4. 수정 방안 구현
        - 올바른 버튼 구성: [Bet, Check, Fold] 또는 [Raise, Call, Fold]
        - Call 금액이 0이면 Check 버튼 표시
        - Raise/Bet 조건 재검토

---

## Compact Log 1
- 20251231 오후

### 요약 내용

**베팅 UI 버튼 구성 버그 수정 완료**

1. 버그 원인 분석
   - 새 베팅 라운드 시작 시 `CurrentMaxBet`는 0으로 리셋되지만, 클라이언트의 `CurrentBet`는 Replication 타이밍으로 인해 이전 값(5) 유지
   - `bCanCheck = (PS->CurrentBet == GS->CurrentMaxBet)` 조건이 false (5 != 0)
   - `CallAmount = max(0, 0-5) = 0`으로 계산되어 "Call 0" 표시
   - 논리적 모순: Call 금액이 0인데 Check가 아닌 Call로 표시

2. 해결 방안 논의
   - 방안 A: 타이머만 추가 (0.05초 딜레이)
   - 방안 B: CurrentMaxBet RepNotify 활용
   - **방안 C 선택**: 로직 수정 + 필요시 타이머 (가장 근본적)

3. UpdateButtonTexts() 로직 수정 (MyPlayerWidget.cpp:84-92)
   - `bCanCheck` 변수 제거
   - Button 1 판단 기준 변경: `CallAmount == 0` 기준으로 Check/Call 결정
   - 포커 룰에 정확히 부합: Call 금액이 0이면 Check
   - `FMath::Max(0, ...)` 덕분에 음수 방어 및 비정상 상황 대응

4. 테스트 결과
   - 동일 상황 재현 시 정상 작동 확인
   - Turn 라운드에서 [Bet 5, Check, Fold] 올바르게 표시
   - 타이머 추가 불필요

### Commit 정보
"fix: 베팅 UI 버튼 로직 개선 - CallAmount 기반 Check/Call 판단"

1. "베팅 UI 버튼 텍스트 로직 수정"
   - bCanCheck 변수 제거, CallAmount 기준으로 Check/Call 판단
   - Call 금액이 0일 때 Check 버튼 표시 (포커 룰 준수)
   - Replication 타이밍 문제 해결

---

