# TTTK 251112

# Start Log
- 20251112 (시간 정보 없음 - 시스템 시간 미제공)
- 작업자 : cubee021

### To Do

1. "현재 프로젝트 상태 파악" :
    1. 기존 코드 구조 분석
        - MyPlayer, MyPlayerController, MyPlayerWidget 클래스 검토
    2. 블루프린트 에셋 구조 확인
    3. 입력 시스템 설정 확인

2. "다음 작업 방향 논의" :
    1. 구현하고자 하는 기능 확인
    2. 멀티플레이 구조 필요 여부 확인
    3. 카드 게임(홀덤) 핵심 로직 설계 방향 결정

## Compact Log 1
- 20251112 22:27
- 작업자 : cubee021

### 요약 내용

**세션 재시작 - PreFlop 카드 배분 시스템 구현 시작**

어제(251111) 완료한 카드 시스템 기초 구현을 바탕으로, 오늘은 게임 페이즈 시스템과 PreFlop 단계까지 구현하여 각 플레이어 앞에 2장의 카드가 나란히 배치되도록 구현 예정.

### 오늘의 구현 계획

1. 게임 페이즈 Enum 정의 (Waiting, PreFlop, Flop, Turn, River, Showdown)
2. HoldemPlayerState 클래스 생성 (HandCards 배열 포함)
3. DrawAndSpawnCard 함수 구현 (덱에서 Pop하며 스폰)
4. 플레이어별 카드 배치 위치 계산 로직
5. PreFlop 페이즈에서 각 플레이어에게 2장씩 카드 배분
6. 멀티플레이 환경에서 카드 배분 테스트

# Start Log
- 20251112 22:27
- 작업자 : cubee021

### To Do

1. "게임 페이즈 Enum 정의" :
    1. EHoldemPhase 생성 (Waiting, PreFlop, Flop, Turn, River, Showdown)
    2. HoldemGameState에 CurrentPhase 추가 (Replicated)

2. "HoldemPlayerState 클래스 생성" :
    1. AHoldemPlayerState 헤더/소스 파일 생성
    2. HandCards TArray<ACard*> 추가 (Replicated)
    3. 필요한 함수 선언 (AddCard, RemoveCard, ClearHand)

3. "DrawAndSpawnCard 함수 구현" :
    1. HoldemGameState에 DrawAndSpawnCard 함수 추가
    2. Deck.Pop() + SpawnCard 통합

4. "플레이어별 카드 배치 위치 계산" :
    1. 플레이어 위치 기준으로 카드 배치 위치 계산 로직
    2. 2장이 나란히 놓이도록 오프셋 설정

5. "PreFlop 카드 배분 구현" :
    1. DealCardsToPlayers 함수 구현
    2. 각 플레이어에게 2장씩 배분
    3. 멀티플레이 동기화 확인

## Compact Log 2
- 20251113 01:38

### 요약 내용

**PreFlop 페이즈 구현 완료**

게임 페이즈 시스템과 PreFlop 단계를 완벽히 구현했습니다. 각 플레이어 앞에 2장의 카드가 플레이어 방향에 맞춰 정확히 배치되며, 멀티플레이 환경에서 서버-클라이언트 간 동기화가 완벽히 작동합니다.

**구현 세부사항:**

1. **게임 페이즈 시스템**
   - `EHoldemPhase` Enum 정의 (Waiting, PreFlop, Flop, Turn, River, Showdown)
   - `HoldemGameState::CurrentPhase` (ReplicatedUsing OnRep)
   - 페이즈 변경 시 로그 출력

2. **HoldemPlayerState 확장**
   - `HandCards` 배열 추가 (서버만 관리, 복제 X)
   - `AddCard()`, `ClearHand()` 함수 구현
   - 족보 판정을 위한 서버 측 카드 추적

3. **카드 스폰 시스템**
   - `DrawAndSpawnCard()`: Deck.Pop() + SpawnCard 통합
   - `GetPlayerCardSpawnLocation()`: 플레이어 기준 카드 배치 위치 계산
   - 카드 간격, 플레이어 거리, 테이블 높이 EditDefaultsOnly로 조정 가능

4. **수학적 회전 계산**
   - `FRotationMatrix::MakeFromYZ()` 사용
   - 플레이어 Forward 벡터 기준 카드 자동 회전
   - 어떤 각도의 좌석에서도 카드가 올바르게 배치됨

5. **GameMode 페이즈 제어**
   - `HoldemGameMode::StartPreFlop()` 구현
   - 덱 생성/셔플 → 카드 배분 흐름 자동화
   - PostLogin에서 2명 이상 접속 시 자동 시작 (테스트용)

6. **멀티플레이 동기화 학습**
   - GameMode vs GameState 역할 분리 이해
   - Replication 원리 (데이터 복제 vs 함수 실행)
   - `HasAuthority()` 체크의 중요성
   - GameMode는 서버 전용이므로 Authority 체크 불필요

### 다음 구현 계획

1. **Flop 페이즈** - 테이블 중앙에 3장 스폰 (고정 위치 방식)
2. **Turn 페이즈** - 테이블에 4번째 카드 추가
3. **River 페이즈** - 테이블에 5번째 카드 추가
4. **Showdown 페이즈** - 구현 방식은 추후 결정 (족보 계산 등)

### Commit 정보

"feat: PreFlop 페이즈 및 카드 배분 시스템 구현

- 게임 페이즈 Enum 정의 (Waiting~Showdown)
- HoldemPlayerState HandCards 관리 시스템
- 수학적 회전 계산으로 카드 자동 배치
- GameMode 페이즈 제어 시스템
- 멀티플레이 카드 배분 동기화

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"

## End Log
- 20251113 01:38
- 작업자 : cubee021

### 오늘 한 일

1. **게임 페이즈 시스템 구축** - Enum 정의 및 Replication 설정
2. **HoldemPlayerState 구현** - 플레이어별 손패 관리
3. **PreFlop 카드 배분 완성** - 수학적 회전 계산으로 정확한 배치
4. **멀티플레이 개념 학습** - GameMode/GameState 역할, Replication 원리 이해
5. **다음 작업 계획 수립** - Flop/Turn/River 구현 방향 결정
