# TTTK 251118

# Start Log
- 20251118 (시각 정보 없음)
- 작업자 : cubee021

### To Do

1. "MyPlayerWidget.cpp 30번째 줄 컴파일 에러 수정" :
    - ✅ 완료 - FText::FromString 형식 지정자 오류 수정
    - FString::Printf를 사용하여 정수를 문자열로 변환 후 FText::FromString에 전달하도록 수정

2. "다음 작업 대기" :
    - 현재 MyPlayerWidget 타이머 표시 기능 정상 동작 확인 필요
    - 추가 기능 구현 또는 버그 수정 요청 대기

## Compact Log 1
- 20251119 00:00

### 요약 내용

BarWidget 아이템 선택 시스템 구현 완료. Waiting Phase에서 플레이어가 체크박스를 통해 아이템을 선택하면 HoldemPlayerState의 SelectedItem이 서버로 전송되어 동기화되도록 구현. PreFlop 페이즈에서 아이템 스폰 로직은 다음 작업으로 이월.

**주요 작업**:
1. BarWidget에 체크박스 선택 로직 구현 (라디오 버튼 방식)
2. HoldemPlayerState에 Server_SetSelectedItem RPC 추가
3. BarWidget에서 PlayerState RPC 호출 연결
4. 네트워킹 개념 학습 (Replicated vs Non-Replicated 차이)

**다음 작업**:
- HoldemGameState::DealPreflopToPlayers()에 아이템 스폰 로직 추가
- 플레이어 앞 300.f 위치에 선택한 아이템 스폰
- GetPlayerCardSpawnLocation() 로직 활용

### Commit 정보
"Implement BarWidget item selection system with PlayerState RPC"

1. "BarWidget 체크박스 선택 시스템 구현"
    - 체크박스 이벤트 핸들러 추가 (Cocktails, Whiskey, Cigar, None)
    - UpdateCheckBoxSelection 헬퍼 함수로 단일 선택 보장 (라디오 버튼 방식)
    - null 체크 추가로 크래시 방지

2. "HoldemPlayerState에 SelectedItem RPC 추가"
    - Server_SetSelectedItem 서버 RPC 구현
    - SelectedItem을 Replicated로 선언하여 자동 동기화

3. "BarWidget과 PlayerState 연결"
    - 각 체크박스 핸들러에서 PlayerState 가져와서 Server RPC 호출
    - GetOwningPlayer()로 PlayerController 참조

4. "버그 수정"
    - MyPlayerWidget.cpp:30 FText::FromString 형식 지정자 오류 수정
    - FString::Printf로 정수를 문자열로 변환 후 전달

## End Log
- 20251119 00:00
- 작업자 : cubee021

### 오늘 한 일

1. "MyPlayerWidget 컴파일 에러 수정"
2. "BarWidget 아이템 선택 시스템 구현"
3. "HoldemPlayerState RPC 네트워킹 구현"
4. "네트워킹 개념 학습 및 설계 논의"

### 다음 세션 할 일

1. "HoldemGameState에 아이템 스폰 로직 구현" :
    1. HoldemGameState.h에 아이템 관련 프로퍼티 추가
        - ItemClass (TSubclassOf<AItem>)
        - ItemSpawnDistanceFromPlayer (300.f)
    2. DealPreflopToPlayers() 함수에 아이템 스폰 로직 추가
        - 모든 플레이어의 PlayerState에서 SelectedItem 가져오기
        - GetPlayerCardSpawnLocation() 로직 참고하여 플레이어 앞 300.f 위치 계산
        - 선택한 아이템 타입에 맞게 AItem 스폰
