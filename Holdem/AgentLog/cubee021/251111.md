# cubee021 251111

# Start Log
- 20251111 15:30 (추정)
- 작업자 : cubee021

### To Do

1. "Camera Pitch Replication 문제 해결"
    1. 현재 문제 분석: Multicast 방식으로 Pitch 복제 시도했으나 클라이언트에서 불안정
    2. 해결 방법 탐색:
        - 방법 1: `bUsePawnControlRotation` 대신 수동 회전 제어
        - 방법 2: SpringArm을 직접 복제하는 방식
        - 방법 3: ControlRotation을 직접 복제
    3. 구현 및 테스트
    4. 멀티플레이 환경에서 검증

2. "마우스 스크롤로 카드 회전 기능 구현"
    1. Input Action (IA_Scroll) 추가
    2. MyPlayerController에 Scroll 입력 처리 추가
    3. InteractableComponent 또는 Card Actor에 회전 함수 구현
    4. Server-Client 동기화 구현
    5. 멀티플레이 환경에서 테스트

## Compact Log 1
- 20251112 (시간 미상)

### 요약 내용

**카드 시스템 기초 구현 완료**

1. **카드 데이터 구조 설계**
   - `FCardData`: 논리적 카드 데이터 (Suit, Rank, CardID)
   - `FCardVisualData`: DataTable용 비주얼 데이터 (FrontMaterial, BackMaterial)
   - `ECardSuit`, `ECardRank`, `ECardState` Enum 정의
   - CardID 계산 로직: `Suit * 13 + Rank` (0~51)

2. **ACard 클래스 확장**
   - Replicated 속성: `CardData`, `CardState`
   - 비복제 속성: `OriginalPosition`, `OriginalRotation` (서버만 관리)
   - `SetCardData()`: CardVisualTable에서 머티리얼 조회 및 적용
   - `ResetToOriginalPosition()`: 카드 원위치 복구 (서버 전용)
   - DataTable 참조 추가로 비주얼 데이터 동적 적용

3. **HoldemGameState 덱 관리 시스템**
   - `GenerateDeck()`: 52장 카드 생성 (4 Suit × 13 Rank)
   - `ShuffleDeck()`: Fisher-Yates 알고리즘으로 랜덤 셔플
   - `SpawnCard()`: FCardData → ACard Actor 스폰
   - `SpawnTestCards()`: 테스트용 카드 스폰 함수
   - Replication 최적화: `Deck`은 서버만, `SpawnedCards`만 복제

4. **멀티플레이 동기화 이슈 해결**
   - 카메라 Pitch 복제 문제: `OnRep_LookPitch`에서 각도 정규화 (0-360 → -180~180)
   - SpringArm `bUsePawnControlRotation` 제어로 원격 캐릭터 시선 동기화
   - Actor Transform 자동 복제 활용으로 Server RPC 최소화

5. **카드 회전 기능**
   - 마우스 휠 입력으로 들고 있는 카드 Roll 축 회전
   - Server RPC로 회전 동기화

### 다음 구현 계획

1. **DrawAndSpawnCard 함수** - 덱에서 Pop하면서 스폰
2. **HoldemPlayerState 생성** - `HandCards` 배열로 플레이어 소유 카드 관리
3. **플레이어별 카드 분배** - 각 플레이어에게 2장씩 분배
4. **게임 페이즈 Enum** - Waiting, PreFlop, Flop, Turn, River, Showdown
5. **테이블 카드 스폰** - 플랍(3장), 턴(1장), 리버(1장) 페이즈별 스폰

### Commit 정보

"feat: 텍사스 홀덤 카드 시스템 기초 구현

- 카드 데이터 구조 및 Enum 정의 (Suit, Rank, State)
- 덱 생성/셔플 및 카드 스폰 시스템 구현
- DataTable 기반 비주얼 적용 시스템
- 멀티플레이 카메라 Pitch 동기화 수정
- 카드 회전 기능 추가 (마우스 휠)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"

