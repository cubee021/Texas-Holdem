# TTTK 251212

# Start Log
- 20251212 10:00
- 작업자 : cubee021

### To Do

1. "MyPlayerWidget 버튼 텍스트 업데이트 시스템 구현" :
    1. IsMyTurn() 헬퍼 함수 추가
        - PlayerArray에서 내 인덱스 찾기
        - CurrentTurnPlayerIndex와 비교
    2. UpdateButtonTexts() 함수 구현
        - CurrentMaxBet, CurrentBet 기반 버튼 텍스트 동적 변경
        - "Bet 20" / "Raise 40" / "Call 10" / "Check" / "Fold"
    3. UpdateBettingUIVisibility() 함수 구현
        - 내 차례일 때: 베팅 UI 표시
        - 내 차례 아닐 때: 베팅 UI 숨김

2. "CurrentTurnPlayerIndex 변경 감지 시스템" :
    1. HoldemGameState.h에 OnRep_CurrentTurnPlayerIndex 추가
        - UPROPERTY ReplicatedUsing 설정
    2. OnRep 함수에서 델리게이트 브로드캐스트
        - OnTurnChanged 델리게이트 선언
        - 모든 클라이언트에서 UI 업데이트 트리거
    3. MyPlayerWidget에서 델리게이트 바인딩
        - BeginPlay에서 OnTurnChanged 바인딩
        - 콜백에서 UpdateBettingUIVisibility() 호출

3. "베팅 UI 정보 표시 기능 추가" :
    1. 현재 팟 금액 표시 (TotalPot)
    2. 현재 최고 베팅액 표시 (CurrentMaxBet)
    3. 내 현재 베팅액 표시 (PlayerState->CurrentBet)
    4. 내 남은 칩 표시 (PlayerState->CurrentChips)
    5. 콜 시 필요한 금액 표시 (CurrentMaxBet - CurrentBet)

4. "UI 테스트 및 검증" :
    1. 2-3명 멀티플레이 환경에서 테스트
    2. 턴이 바뀔 때 UI 표시/숨김 확인
    3. 버튼 텍스트가 상황에 맞게 변경되는지 확인
    4. Replication 확인 (모든 클라이언트 동기화)

---

## Compact Log 1
- 20251212 18:30

### 요약 내용

#### 1. UI 자동 업데이트 시스템 구축 (이벤트 기반 아키텍처)

**핵심 설계:**
- Tick 방식 대신 **델리게이트 기반 이벤트 시스템** 구현
- 턴 변경 시 자동으로 모든 클라이언트 UI 업데이트

**전체 흐름:**
```
[서버] CurrentTurnPlayerIndex 변경
    ↓
    ├─→ [클라이언트] Replication → OnRep_CurrentTurnPlayerIndex() → OnTurnChanged.Broadcast()
    └─→ [서버] 명시적 OnTurnChanged.Broadcast()
    ↓
[모든 플레이어] OnTurnChangedHandler() 호출
    ├─→ UpdateBettingUIVisibility() (내 차례면 UI 표시)
    └─→ UpdateButtonTexts() (버튼 텍스트 동적 변경)
```

#### 2. HoldemGameState - 턴 변경 감지 시스템

**변경사항:**
```cpp
// 델리게이트 선언
DECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(FOnTurnChanged, int32, NewTurnIndex);

// CurrentTurnPlayerIndex에 OnRep 추가
UPROPERTY(ReplicatedUsing=OnRep_CurrentTurnPlayerIndex)
int32 CurrentTurnPlayerIndex = -1;

UFUNCTION()
void OnRep_CurrentTurnPlayerIndex();

UPROPERTY(BlueprintAssignable)
FOnTurnChanged OnTurnChanged;
```

**HoldemGameState.cpp 구현:**
```cpp
void AHoldemGameState::OnRep_CurrentTurnPlayerIndex()
{
    OnTurnChanged.Broadcast(CurrentTurnPlayerIndex);
}
```

#### 3. HoldemGameMode - 서버에서도 델리게이트 Broadcast

**문제점:**
- OnRep는 클라이언트에서만 호출됨
- 서버(리슨 서버)도 UI를 받으려면 명시적 Broadcast 필요

**해결:**
```cpp
// MoveToNextPlayer() - 다음 플레이어로 이동 시
if (NextPlayer && !NextPlayer->bIsFolded)
{
    GS->OnTurnChanged.Broadcast(GS->CurrentTurnPlayerIndex);
    return;
}

// MoveToNextPlayer() - 베팅 라운드 완료 시
GS->CurrentTurnPlayerIndex = -1;
GS->OnTurnChanged.Broadcast(-1);

// StartBettingRound() - 새 베팅 라운드 시작 시 (버그 수정)
GS->CurrentTurnPlayerIndex = 0;
// ... Fold 플레이어 스킵 로직 ...
GS->OnTurnChanged.Broadcast(GS->CurrentTurnPlayerIndex);
```

#### 4. MyPlayerController - IsMyTurn() 헬퍼 함수

**설계 결정:**
- 처음에는 MyPlayerWidget에 구현 예정이었으나
- PlayerController가 더 적합하다고 판단 (RPC에서도 재사용 가능)

**MyPlayerController.h:**
```cpp
public:
    //---------------------------------------------------//
    // Betting Helpers
    //---------------------------------------------------//
    UFUNCTION(BlueprintCallable, Category = "Betting")
    bool IsMyTurn() const;
```

**MyPlayerController.cpp:**
```cpp
bool AMyPlayerController::IsMyTurn() const
{
    AHoldemGameState* GS = GetWorld()->GetGameState<AHoldemGameState>();
    AHoldemPlayerState* PS = GetPlayerState<AHoldemPlayerState>();
    if (!GS || !PS) return false;

    int32 MyIndex = GS->PlayerArray.Find(PS);
    if (MyIndex == INDEX_NONE) return false;

    return GS->CurrentTurnPlayerIndex == MyIndex;
}
```

**리팩토링:**
- 기존 RPC 함수들(Server_Fold 등)에서 중복 턴 체크 로직을 `IsMyTurn()` 호출로 간소화

#### 5. MyPlayerWidget - 델리게이트 바인딩 및 핸들러

**MyPlayerWidget.h:**
```cpp
protected:
    // 턴 변경 이벤트 핸들러
    UFUNCTION()
    void OnTurnChangedHandler(int32 NewTurnIndex);

public:
    UFUNCTION(BlueprintCallable)
    void UpdateBettingUIVisibility();
```

**NativeConstruct()에서 델리게이트 바인딩:**
```cpp
void UMyPlayerWidget::NativeConstruct()
{
    // ... 기존 코드 ...

    // OnTurnChanged 델리게이트 바인딩
    if (GS)
    {
        GS->OnTurnChanged.AddDynamic(this, &UMyPlayerWidget::OnTurnChangedHandler);
    }
}
```

**OnTurnChangedHandler() 구현:**
```cpp
void UMyPlayerWidget::OnTurnChangedHandler(int32 NewTurnIndex)
{
    // UI 표시/숨김 업데이트
    UpdateBettingUIVisibility();

    // 내 차례일 때만 버튼 텍스트 업데이트
    AMyPlayerController* PC = Cast<AMyPlayerController>(GetOwningPlayer());
    if (PC && PC->IsMyTurn())
    {
        UpdateButtonTexts();
    }
}
```

#### 6. MyPlayerWidget - UI 업데이트 로직

**UpdateBettingUIVisibility() 구현:**
```cpp
void UMyPlayerWidget::UpdateBettingUIVisibility()
{
    if (!VtclBox_Betting) return;

    AMyPlayerController* PC = Cast<AMyPlayerController>(GetOwningPlayer());
    if (!PC) return;

    // 내 차례이고, 베팅 Phase일 때만 표시
    bool bShouldShowUI = PC->IsMyTurn() && GS &&
        (GS->CurrentPhase == EHoldemPhase::PreFlop ||
         GS->CurrentPhase == EHoldemPhase::Flop ||
         GS->CurrentPhase == EHoldemPhase::Turn ||
         GS->CurrentPhase == EHoldemPhase::River);

    VtclBox_Betting->SetVisibility(
        bShouldShowUI ? ESlateVisibility::Visible : ESlateVisibility::Hidden
    );
}
```

**UpdateButtonTexts() 구현:**
```cpp
void UMyPlayerWidget::UpdateButtonTexts()
{
    AHoldemPlayerState* PS = GetOwningPlayerState<AHoldemPlayerState>();
    if (!PS || !GS) return;

    // 베팅 상황 체크
    bool bNoBets = (GS->CurrentMaxBet == 0);
    bool bCanCheck = (PS->CurrentBet == GS->CurrentMaxBet);
    int32 CallAmount = GS->CurrentMaxBet - PS->CurrentBet;

    // Button 0: Raise or Bet
    if (bNoBets)
    {
        FString BetText = FString::Printf(TEXT("Bet %d"), GS->BigBlindAmount);
        Txt_0->SetText(FText::FromString(BetText));
    }
    else
    {
        int32 RaiseAmount = GS->BigBlindAmount * 2;
        FString RaiseText = FString::Printf(TEXT("Raise %d"), RaiseAmount);
        Txt_0->SetText(FText::FromString(RaiseText));
    }

    // Button 1: Call or Check
    if (bCanCheck)
    {
        Txt_1->SetText(FText::FromString("Check"));
    }
    else
    {
        FString CallText = FString::Printf(TEXT("Call %d"), CallAmount);
        Txt_1->SetText(FText::FromString(CallText));
    }

    // Button 2: Fold
    Txt_2->SetText(FText::FromString("Fold"));
}
```

#### 7. 발견된 버그 및 해결

**버그 1: Flop 이후 베팅이 시작되지 않음**
- **원인:** `StartBettingRound()`에서 OnTurnChanged.Broadcast() 누락
- **해결:** StartBettingRound() 끝에 `GS->OnTurnChanged.Broadcast(GS->CurrentTurnPlayerIndex);` 추가

**버그 2: 버튼 텍스트와 RPC 로직 불일치 (미해결)**
- **증상:** Txt_0이 "Bet 20"으로 표시되는데, 누르면 Raise 로직이 실행됨
- **상태:** 다음 세션에서 해결 예정

#### 8. 테스트 결과

**동작 확인:**
- ✅ PreFlop → Flop → Turn → River 순서대로 베팅 라운드 진행
- ✅ 턴이 바뀔 때마다 UI 자동 표시/숨김
- ✅ W, S 키로 버튼 선택 가능
- ✅ E 키로 베팅 확인
- ✅ 버튼 선택 시 노란색 하이라이트 표시

**미완성 항목:**
- ❌ 버튼 텍스트와 실제 RPC 로직 일치시키기
- ❌ 베팅 정보 표시 UI (팟, 칩, 콜금액)
- ❌ 멀티플레이 환경 본격 테스트

### 다음 세션 작업 항목

#### 1. 버튼 텍스트 - RPC 로직 불일치 버그 수정

**문제:**
- `UpdateButtonTexts()`에서 표시되는 텍스트와 `OnBettingConfirm()`에서 실행되는 RPC가 다름

**예상 원인:**
```cpp
// OnBettingConfirm() 현재 로직
switch (SelectedButtonIndex)
{
case 0:  Server_Raise(); break;  // 항상 Raise?
case 1:  bCanCheck ? Server_Check() : Server_Call(); break;
case 2:  Server_Fold(); break;
}
```

**해결 방향:**
- Button 0이 "Bet"일 때는 Server_Raise가 아닌 다른 로직이어야 함
- 또는 UpdateButtonTexts() 로직을 OnBettingConfirm()과 일치시키기

#### 2. 베팅 정보 표시 UI 추가

**추가할 UI 요소:**
1. **현재 팟 금액** - `GS->TotalPot` 표시
2. **현재 최고 베팅액** - `GS->CurrentMaxBet` 표시
3. **내 현재 베팅액** - `PS->CurrentBet` 표시
4. **내 남은 칩** - `PS->CurrentChips` 표시
5. **콜 시 필요 금액** - `CallAmount` 표시

**구현 방법:**
- MyPlayerWidget.h에 TextBlock 변수 추가 (BindWidget)
- UpdateBettingInfo() 함수 구현
- OnTurnChangedHandler()에서 호출

#### 3. Dealer/Blind 포지션 시스템

**현재 문제:**
- 모든 베팅 라운드가 0번 플레이어부터 시작
- Blind 강제 베팅 없음

**구현할 사항:**
- Dealer, SmallBlind, BigBlind 포지션 할당
- PreFlop: BigBlind 다음부터 시작
- Flop/Turn/River: SmallBlind부터 시작
- Blind 강제 베팅 적용

#### 4. 베팅 로직 개선

**추가 기능:**
- Raise 금액 조절 (현재는 BigBlindAmount * 2 고정)
- All-In 처리
- Side Pot 시스템

#### 5. 멀티플레이 환경 테스트

**테스트 시나리오:**
- 2-3명 플레이어 동시 접속
- Raise → 다른 플레이어들 bHasActedThisRound 리셋 확인
- Fold → 턴 건너뛰기 확인
- 모든 클라이언트에서 UI 동기화 확인

#### 6. UI/UX 개선

**추가 고려사항:**
- 베팅 타이머 (일정 시간 내 베팅 강제)
- 칩 이동 애니메이션
- 카드 뒤집기 애니메이션
- 사운드 효과

### Commit 정보

"feat: UI 자동 업데이트 시스템 구현 (이벤트 기반)"

1. "GameState 턴 변경 감지 시스템"
   - CurrentTurnPlayerIndex에 OnRep_CurrentTurnPlayerIndex 추가
   - OnTurnChanged 델리게이트 선언 및 브로드캐스트
   - 클라이언트 자동 UI 업데이트 가능

2. "GameMode 서버 델리게이트 브로드캐스트"
   - MoveToNextPlayer()에서 OnTurnChanged 브로드캐스트
   - StartBettingRound()에서 OnTurnChanged 브로드캐스트
   - 서버도 델리게이트 받을 수 있도록 수정

3. "PlayerController IsMyTurn() 헬퍼 함수"
   - IsMyTurn() 함수 추가 (PlayerArray.Find 사용)
   - 기존 RPC 함수들 리팩토링 (중복 코드 제거)

4. "PlayerWidget UI 자동 업데이트 시스템"
   - OnTurnChanged 델리게이트 바인딩
   - OnTurnChangedHandler() 구현
   - UpdateBettingUIVisibility() 구현 (내 차례만 UI 표시)
   - UpdateButtonTexts() 구현 (동적 텍스트 변경)

---

## End Log
- 20251212 18:30
- 작업자 : cubee021

### 오늘 한 일

1. "UI 자동 업데이트 시스템 구축"
   - 이벤트 기반 아키텍처 구현 (Tick 대신 델리게이트)
   - OnRep + 델리게이트 조합으로 턴 변경 감지

2. "턴 변경 감지 시스템 구현"
   - HoldemGameState에 OnRep_CurrentTurnPlayerIndex 추가
   - OnTurnChanged 델리게이트 선언 및 브로드캐스트
   - 서버/클라이언트 모두 델리게이트 수신

3. "IsMyTurn() 헬퍼 함수"
   - MyPlayerController에 구현 (설계 논의 후 위치 결정)
   - 기존 RPC 코드 리팩토링

4. "베팅 UI 자동 업데이트"
   - OnTurnChangedHandler 구현
   - UpdateBettingUIVisibility() (내 차례만 UI 표시)
   - UpdateButtonTexts() (버튼 텍스트 동적 변경)

5. "버그 수정"
   - StartBettingRound()에서 델리게이트 브로드캐스트 누락 해결
   - Flop 이후 베팅 시작 안 되는 문제 해결

### 다음 세션 작업 예정

1. 버튼 텍스트 - RPC 로직 불일치 버그 수정
2. 베팅 정보 표시 UI 추가 (팟, 칩, 콜금액)
3. Dealer/Blind 포지션 시스템 구현
4. 멀티플레이 환경 테스트
5. UI/UX 개선 (애니메이션, 사운드)

