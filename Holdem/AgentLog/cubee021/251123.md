# TTTK 251123

# Start Log
- 20251123 14:08
- 작업자 : cubee021

### To Do

1. "BarWidget 아이템 선택 시스템 확인 및 PreFlop 아이템 스폰 구현" :
    - 이전 세션(251118)에서 BarWidget 선택 시스템과 아이템 스폰 기본 구현 완료
    - 실제 아이템이 스폰되는지 확인 및 보완

2. "Waiting 페이즈에서만 BarWidget/타이머 표시 제한" :
    - MyPlayerController에 페이즈 체크 로직 추가
    - BarWidget 열기/닫기 동작 개선

3. "세션 나가기 기능 구현" :
    - Back 버튼으로 서버/클라이언트 세션 나가기
    - 세션 정리 문제 해결

4. "InputMode 및 위젯 토글 문제 해결" :
    - FInputModeGameAndUI 적용
    - 델리게이트 중복 바인딩 문제 해결

## Compact Log 1
- 20251123 14:08

### 요약 내용

**PreFlop 아이템 스폰 시스템 구현 완료**
- HoldemGameState에 `SpawnPlayerItem()` 함수 구현
- PlayerState의 SelectedItem 값에 따라 플레이어 앞에 AItem 스폰
- ItemType 설정 및 서버/클라이언트 메시 동기화 처리
- None 타입 체크, null 체크, SpawnParams 설정 등 안정성 확보

**Waiting 페이즈 제한 기능**
- MyPlayerController::ShowItemWidget()에 CurrentPhase 체크 추가
- Waiting 페이즈에서만 BarWidget 열 수 있도록 제한

**BarWidget 개선**
- NativeConstruct()에서 None 체크박스 기본 선택 구현
- PlayerState에 기본값 설정

**세션 나가기 기능 구현**
- BarWidget Back 버튼으로 세션 나가기
- 서버: DestroyMySession() 호출 후 메인 메뉴 이동
- 클라이언트: LeaveSession() 호출 (세션 정리 문제는 보류)
- GameInstance에 세션 파괴 함수 및 콜백 추가

**InputMode 및 위젯 토글 문제 해결**
- `FInputModeUIOnly()` 문제: 게임 입력 차단으로 스페이스바 토글 불가
- `FInputModeGameAndUI()` 적용으로 해결
- Look() 함수에 `if (bItemWidgetOn) return;` 추가하여 위젯 열렸을 때 카메라 회전 막기
- 델리게이트 중복 바인딩 문제: `RemoveFromParent()` → `SetVisibility()` 변경으로 해결

### Commit 정보
"Implement PreFlop item spawn system and improve BarWidget UX"

1. "HoldemGameState 아이템 스폰 구현"
    - SpawnPlayerItem() 함수 추가
    - PlayerState의 SelectedItem에 따라 아이템 스폰
    - 서버에서 ItemType 설정 및 SetItemMesh() 호출
    - None 타입, null 체크, SpawnParams 등 안정성 처리

2. "BarWidget 개선"
    - NativeConstruct()에서 None 체크박스 기본 선택
    - Waiting 페이즈에서만 열 수 있도록 제한

3. "세션 나가기 기능 구현"
    - Back 버튼으로 서버/클라이언트 세션 나가기
    - GameInstance에 DestroyMySession(), LeaveSession() 추가
    - OnDestroySessionComplete 콜백 구현

4. "InputMode 및 위젯 토글 개선"
    - FInputModeGameAndUI 사용
    - Look() 함수에 bItemWidgetOn 체크 추가
    - SetVisibility() 사용으로 델리게이트 중복 바인딩 해결

## End Log
- 20251123 14:08
- 작업자 : cubee021

### 오늘 한 일

1. "PreFlop 아이템 스폰 시스템 구현"
2. "BarWidget UX 개선 (페이즈 제한, 기본 선택)"
3. "세션 나가기 기능 구현"
4. "InputMode 문제 해결 및 위젯 토글 개선"

### 다음 세션 할 일

1. "Waiting→PreFlop 전환 시 BarWidget 자동 닫기" :
    - HoldemGameState의 OnRep_CurrentPhase() 또는 OnPhaseChanged 델리게이트 활용
    - PreFlop으로 전환될 때 모든 플레이어의 BarWidget 자동으로 닫기
    - MyPlayerController에서 페이즈 변경 감지 및 처리

2. "BarWidget의 Txt_Name, Txt_SessionName 바인딩" :
    - Txt_Name: 플레이어 이름 표시 (PlayerState의 PlayerName)
    - Txt_SessionName: 세션 이름 표시 (GameInstance의 currSessionName 또는 세션 정보)
    - NativeConstruct() 또는 NativeTick()에서 텍스트 업데이트

3. "세션 처음 입장 시 InputModeGameOnly 설정" :
    - 클라이언트가 게임 맵에 입장했을 때 InputMode 초기화
    - PlayerController BeginPlay() 또는 OnPossess()에서 설정

4. "(보류) 클라이언트 세션 나가기 문제 해결" :
    - currSessionName 확인 및 세션 정리 로직 디버깅
